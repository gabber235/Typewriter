apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
  - certificate.yaml
  - ingress.yaml
patches:
  - path: workloaddeployment-patch.yaml
    target:
      kind: WorkloadDeployment
      name: auth-sentinel
