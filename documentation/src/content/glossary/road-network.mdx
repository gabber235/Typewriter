---
title: Road network
aliases: ["road network", "road networks"]
---

A **road network** is a graph of nodes and edges Typewriter uses to path
efficiently between two distant points, so NPCs can walk sensible routes across
your map instead of computing paths block by block.
